#SIMPLE MATHEMATICAL OPERATORS

#ADDITION

a,b=1,2
a+b

#add
import operator        
operator.add(a, b)

#EXPONENTIATION
a, b = 2, 3
(a ** b)               # = 8 pow(a, b)              # = 8
import math math.pow(a, b)         # = 8.0 (always float; does not allow complex results)
import operator operator.pow(a, b)     # = 8
